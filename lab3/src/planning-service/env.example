# =============================================================================
# PLANNING SERVICE - ENVIRONMENT VARIABLES
# =============================================================================

# Database Configuration
DATABASE_URL=postgresql://budget_user:budget_password@postgres:5432/budget_db

# Alternative database settings (for local development)
# DATABASE_URL=postgresql://budget_user:budget_password@localhost:5432/budget_db

# Planning Service Server Settings
PLANNING_SERVICE_HOST=0.0.0.0
PLANNING_SERVICE_PORT=8080

# In-memory mode (fallback when database is unavailable)
USE_IN_MEMORY=false

# Development Settings
DEBUG=false
LOG_LEVEL=INFO

# Alembic Database URL (for migrations)
# Used by alembic.ini if you want to override the default
# ALEMBIC_DATABASE_URL=postgresql://budget_user:budget_password@localhost:5432/budget_db 